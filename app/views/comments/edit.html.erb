<h1>Edytuj swój komentarz</h1>

<%= form_tag :controller => "comments", :action => "update" do %>
  <%= text_area_tag(:content, @comment.content) %>
  <%= hidden_field_tag(:event_id, @event.id) %>
  <%= hidden_field_tag(:user_id, @current_user.id) %>
  <%= submit_tag("dodaj") %>
<%end%>

