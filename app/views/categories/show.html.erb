<p id="notice"><%= notice %></p>

<p>
  <b>Nazwa kategorii:</b>
  <%= @category.name %>
</p>

<p>
 <b> Wydarzenia tej kategorii </b><br/>
 <% if !@events.empty? %>
 <ul>
 	<% @events.each do |event| %>
 		<li> <%= link_to event.name, event %> <% if @current_user && @current_user.events.include?(event) %> - Jestes zapisany na to wydarzenie<% end %></li>
 	<% end %>
 </ul>
 <% else %>
 Brak wydarzeń tej kategorii
 <% end %>
</p>
  
 	

<% if @current_user && @current_user.admin %>
  <%= link_to 'Edytuj', edit_category_path(@category) %> |
<% end %>
<%= link_to 'Wróć', categories_path %>
